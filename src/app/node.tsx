type GNodeConfig = {
    x: number;
    y: number;
}

const GNode = (config: GNodeConfig) => {
    return (
        <div>
            Total show;
        </div>
    )
}

export {
    GNode,
    GNodeConfig,
}